#' This function calls CBS on a matrix of minimum distances to calculate copy number segments
#'
#' This function will return a single GRanges object containing the copy number segments of all families in input
#' @param md The minimum distance matrix produced by calcMD
#' @param bins The set of bins determined by calcBins
#' @keywords filter
#' @export
filter = function(cbs){
}